<?php

namespace Database\Seeders;

use App\Models\Role;
use App\Models\User;
use Illuminate\Database\Seeder;

class UserRoleSeeder extends Seeder
{
    /**
     * Run the database seeds.
     *
     * @return void
     */
    public function run()
    {
//        $role = User::all();
//
//        Role::all()->each(function ($user) use ($role){
//            $user->role()->attach(
//                $role->random(1)->pluck('id')
//            );
//        });
    }
}
